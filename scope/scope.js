function greetSomeone(firstName) {
    let time = 'night';
    if(time === 'night') {
        let greeting = 'Good Night';
        debugger;
    }

    return greeting + ' ' + firstName;
}

greetSomeone('Steve');